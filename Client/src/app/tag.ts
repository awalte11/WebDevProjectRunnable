export class tag
{
	name: string; //name of tag is primary key of tag
	/**
	*not sure if pointers to containing things should go here or be queried from server when actually used. will need to know more about integration first
	*this is user-side data object
	*users don't need to be sent a list of everything a tag is on, when they're just viewing the tags on a thing as part of looking at it
	*/
	
}